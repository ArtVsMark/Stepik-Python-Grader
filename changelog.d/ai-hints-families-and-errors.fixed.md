AI-подсказки перестали молча отключаться: `gpt-5`/`gpt-5.1`/`gpt-5-mini` и `deepseek-reasoner` теперь распознаются как reasoning-семейства и получают `max_completion_tokens` вместо `max_tokens`+`temperature` — прежде провайдер отвергал такой запрос целиком, а пользователь видел пустоту. Отказ провайдера тоже перестал быть немым: 401, 429, 400 и обрыв сети возвращаются причиной рядом с подсказкой (`AiHintOutcome.reason`), CLI печатает её человеческой строкой, `/api/v1/hint` отдаёт полем `reason` — раньше «ключ не принят» выглядело ровно как «канал выключен» (#1123)
