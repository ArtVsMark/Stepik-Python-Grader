XSS-регрессия веб-вывода закрывает три стока вместо одного: кроме вывода решения — имя каталога в колонке «Файл» (шаблон `task*.py` ограничивает имя файла, а имя папки не ограничено ничем) и путь, введённый пользователем, в тексте ошибки. Проверено подменой: сломанное `esc()` краснит все три, прежняя редакция заметила бы только первый. Файл переведён на русский по инварианту языка артефактов, а граница зелёного названа вслух — сверки всех вызовов `esc()` по веб-слою по-прежнему не существует (#1442)
