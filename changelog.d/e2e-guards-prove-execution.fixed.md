Сторожа набора перестали быть слепыми: инвентарь пропусков видит `pytestmark` (пропуск целого файла прежде не попадал в отчёт вовсе), e2e-guard проверяет ВЫПОЛНЕННЫЕ тесты вместо собранных, сломанный `playwright` под `STEPIK_REQUIRE_E2E_TESTS=1` даёт ошибку вместо пропуска самих guard'ов, а тест «Повторить» доказывает повторный запрос, а не только исчезнувший баннер (#921)
