Сборка из дерева без `.git` больше не падает `LookupError`: у `setuptools-scm` появилась запасная версия `0.0.0`. Так исходники получают чаще, чем полным клоном — «Download ZIP» с GitHub, `git archive`, вендоренная копия, образ с `.dockerignore` на `.git`, — и до сих пор пакет в этих случаях не собирался вовсе, а сообщение не объясняло причину (#1125)
