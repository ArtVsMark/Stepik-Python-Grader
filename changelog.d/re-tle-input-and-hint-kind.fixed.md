При `RE` и `TLE` подробный отчёт снова показывает вход кейса — ветка ошибки печатала только текст исключения и обрывала вывод, хотя именно там «на каких данных сломалось» нужнее всего; для `TLE` добавлены `Expected`/`Actual`. Заодно RE-подсказка перестала выдавать карточку функции или термина за объяснение падения: имя берётся из последней строки stderr, и `sys.exit("input: неверный формат")` приносил студенту описание функции `input()` (#1184)
