Гейт `preflight.py` перестал быть непроходимым вне Linux: `mypy` зовётся с платформой CI, поэтому семь ошибок про `fcntl.flock` и `signal.SIGKILL`, которых в прогоне `static` не существует, больше не красят локальный прогон на Windows и macOS (#1286)
