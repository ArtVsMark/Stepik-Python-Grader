Реестр точек дампа: `scripts/check_secret_dumps.py` перечисляет все места, где сетевые модули пишут на диск, и требует по каждому причину, почему туда не попадёт секрет; новая точка без разбора не проходит CI. Заодно закрыта единственная найденная дыра — файловый кэш ответов Stepik API складывал ответ целиком без редакции и теперь отказывается кэшировать ответ, в котором редакция что-то изменила бы (#1301)
