После полного удаления истории продукт называет базу, которая стала активной: резолв идёт по списку (локальная `.grader_history.db` → общая `~/.stepik-grader/history.db`), и удаление локальной освобождало место общей — `--insights` следом печатал чужие задачи, что выглядело как невыполненная команда. Чужая история при этом не удаляется: в ней прогоны других папок, поэтому она названа, а не стёрта (#996)
