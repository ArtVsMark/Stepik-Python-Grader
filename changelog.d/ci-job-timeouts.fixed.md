У каждого job'а CI и релиза появился свой `timeout-minutes`. Без него действует умолчание GitHub — шесть часов, и зависший шаг держит не только свой job: 19.08 `e2e` встал на установке Playwright и три с половиной часа не отпускал прогон `main`, а с ним всю очередь мержа — при этом ни одна проверка не была красной, гейт честно читал «идёт прогон, ждите». `check_workflow_guardrails.py` теперь роняет прогон на job'е без таймаута (#PR)
