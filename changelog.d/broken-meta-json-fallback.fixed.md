Повреждённый `meta.json` рядом с задачей больше не роняет прогон: не-UTF8 байт (`UnicodeDecodeError`) и JSON с массивом в корне шли мимо прежнего перехвата и в режиме 2 уносили с собой результаты остальных решений. Файл — подсказка про имя функции, а не источник истины, поэтому его порча откатывает к эвристике по AST. Проверены все одиннадцать вызовов `load_json_file`: узкий перехват был только здесь (#1197)
