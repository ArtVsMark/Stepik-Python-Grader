Окружение решения больше не получает дескрипторы доступа: `SSH_AUTH_SOCK` (сокет ssh-агента — подпись чем угодно ключами пользователя без раскрытия ключа), `XAUTHORITY`, `GNUPGHOME`, `KUBECONFIG` и адреса агентов. Прежний denylist искал в имени слова «секрет», «токен», «пароль» — и пропускал ровно то, что секрета в значении не содержит, а доступ даёт (#996)
