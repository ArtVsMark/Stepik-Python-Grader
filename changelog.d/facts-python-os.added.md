`facts.json` отдаёт `python.os` — список операционных систем матрицы, выведенный из `ci.yml`, и `schema_of` рядом со `schema`. Витрина добывала число ОС регулярным выражением по именам наших джобов, то есть держала знание о нашем формате имён в своём коде: переименуй мы комбинацию — у соседа молча изменилось бы число, и не упало бы ничего (#1452)
