Правило про интерпретатор в облачном окне названо конкретно: `gh_rest.py` и всё, что ходит в GitHub, — системным `python` (строгой проверки CA там нет), а `preflight.py` и линтеры — `.venv/bin/python` (на системном 3.11 они падают `SyntaxError`). Общая формулировка «тем интерпретатором, где флаг выключен» не спасала: правило для двух интерпретаторов противоположное, а отказ транспорта выглядит как проблема с правами, а не с версией Python
