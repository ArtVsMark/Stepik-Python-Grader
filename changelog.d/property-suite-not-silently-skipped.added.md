Property-набор больше не может исчезнуть молча: пропажа `hypothesis` превращала восемь тестов в «8 skipped» с кодом возврата 0, теперь это красный отказ (`tests/test_property_not_silently_skipped.py`). Заодно ассерт e2e-теста о потерянном прогоне назвал конкретное сообщение вместо «слова в любой из двух локалей»: язык стенда детерминированно русский, и посылка об этом теперь проверяется, а не подразумевается (#1435)
