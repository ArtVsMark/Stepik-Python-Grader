Веб отдаёт условие скачанной задачи: `GET /api/task/statement` — шапка, тело с серверной очисткой HTML по whitelist и список вложений, `GET /api/task/image` — картинки-вложения по списку из `meta.json` (#1223)
