Переопределение конфига в рантайме (`--config`, флаги CLI) снова доходит до трёх мест, читавших снимок на импорте: имя env-переменной с AI-ключом при чистке окружения решения — из-за чего ключ мог уехать недоверенному коду, — дефолтный лимит памяти и тумблеры статистики и истории в интерактивном меню (#996)
