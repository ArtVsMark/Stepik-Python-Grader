Хуки харнесса попали в репозиторий: `.gitignore` игнорировал `.claude/*` целиком, поэтому объявленный в настройках `SessionStart` существовал только в рабочей копии — в чистом клоне и в CI механизма не было, а настройки его честно объявляли; теперь расхождение ловит `check_rules_digest.py` (#1384)
