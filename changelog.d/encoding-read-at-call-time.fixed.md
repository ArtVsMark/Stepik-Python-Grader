Настройка `encoding` снова действует на загрузку тестов и детекцию режима: оба модуля связывали `CONFIG.encoding` на импорте, поэтому `--config` и `override_config()` до них не доходили — пользователь с файлами курса в cp1251 ставил кодировку, и она молча не применялась. Значение читается в момент вызова, как таймаут в grader_core (#996)
