Гейт называет, чей код он проверяет: в `git worktree` пакет импортируется из основного клона (`pip install -e` ставит его на весь интерпретатор), и прогон честно отрабатывал гибрид «мои тесты против чужого кода» — за одну сессию это дало и ложно-зелёный (правка ядра не участвовала), и ложно-красный (чужой тест из третьей ветки). Теперь расхождение видно строкой в отчёте, но пушу не мешает: worktree для `scripts/`, `tests/` и документации законен (#1521)
