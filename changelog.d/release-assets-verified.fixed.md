Релиз перестал молча выходить без файлов: у публикации включён `fail_on_unmatched_files`, а собранное колесо проверяется на содержимое (`scripts/check_wheel_contents.py`) — вендор-бандл, шрифты, локали, данные глоссария и `py.typed`. Прежде пустой glob давал зелёный job и страницу релиза без единого ассета, а сломанный `package-data` не видели ни `twine check`, ни тесты: они читают дерево исходников, а не артефакт (#1127)
