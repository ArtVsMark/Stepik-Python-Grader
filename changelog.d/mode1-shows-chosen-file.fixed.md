В режиме 1 результат подписан выбранным решением, а не временным файлом: код из редактора по-прежнему исполняется из `tmpXXXXXX.py` рядом с задачей (#297 — чтобы не было гонки `save → grade`), но в таблице, разборе и в истории стоит имя, которое человек выбрал. Прежняя подпись обесценивала верный вердикт — по ней и убеждаются, что проверено именно то решение, а `tmpXXXXXX.py` на скриншоте для ментора выглядел сбоем инструмента. Реальный путь исполнения не подменяется: он нужен диагностике (#1211)
