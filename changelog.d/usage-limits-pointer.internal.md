`CLAUDE.md` § Гейты называет, чем смотреть остаток лимитов Claude: это ДРУГОЙ остаток, чем квота GitHub API, и путать их нельзя — числом по нему отвечает соседний инструмент Claude-Code_Usage-Token, реестр сессий даёт только светофор. Указатель, а не зависимость: ни подмодуля, ни копии, ни импорта (#1320)
