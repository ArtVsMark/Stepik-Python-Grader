Настройки научились хранить именованные профили запуска (`launch_profiles`, `last_profile`) — ядро для выбора «как обычно» / «с изоляцией» одним контролом вместо пяти; операции над профилями идут под той же блокировкой, что и остальная запись, а негодные имена отвергаются, а не подменяются молча (#1133)
