Сбой выгрузки артефакта больше не красит `main` при зелёных тестах: выгрузка несёт диагностику, её потребители и так переживают недостачу, а вердикт остаётся за прогоном тестов — держит новый гейт `check_workflow_guardrails.py` (#1509)
