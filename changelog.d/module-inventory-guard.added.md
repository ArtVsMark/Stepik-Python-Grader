Таблица модулей в `docs/dev/architecture.md` сверяется с деревом в обе стороны: одиннадцать модулей строки не имели вовсе — `core/diagnostics.py`, `cli/exit_codes.py`, три веб-адаптера и другие, — и заметить это было нечем, потому что сверялись только рёбра графа. Строки дописаны, расхождение стало красным (#1437)
