# Прогон по курсам: три роли, сверка со Stepik, наполнение базы

> Протокол **фазы L** — самой ценной части двухоконного аудита
> ([`environments.md`](environments.md)). Выполняется на локальной машине, где
> есть сеть до Stepik, реальный токен и пройденные курсы владельца.
>
> Не путать с [`local-sweep.md`](local-sweep.md): тот прогоняет подсистемы по
> **уже скачанной** базе. Здесь база **создаётся** — шаг за шагом, вместе с
> историей обучения.

---

## Зачем это, если есть корпус и тесты

Две вещи, которых не даёт больше ничто.

**1. Внешний судья вердикта.** До сих пор грейдер проверялся против самого
себя: тесты написаны нами, ожидания в каталоге мутаций написаны человеком,
эталонные вердикты — тоже наши. Stepik судит независимо. Расхождение локального
вердикта с вердиктом платформы — единственное доказательство, которое нельзя
оспорить как «неверное ожидание в стенде».

**2. Настоящая база истории.** «Подучить», «Прогресс», серия, бейджи, карточки
ошибок и AI-подсказки строятся поверх накопленных данных. Сейчас они проверяются
на трёх записях фикстуры — то есть на входе, которого в жизни не бывает. Прогон
по курсам наполняет базу **реальными паттернами ошибок**, и только на них видно,
осмысленно ли работает то, что показывается пользователю.

---

## Материал: курсы

| Курс | ID | Состояние |
|---|---|---|
| Поколение Python: для начинающих | `58852` | пройден полностью |
| Поколение Python: для продвинутых | `68343` | пройден полностью |
| Поколение Python: для профессионалов | `82541` | **не пройден** (отложен ради этого проекта) |
| Дополнительный курс | `227004` | пройден |
| Дополнительный курс | `174852` | пройден |

Состояние курса определяет, **можно ли отправлять решения** — см. § Границы.

---

## Три роли-решателя

Каждый шаг с окном для кода решается трижды. Роли — не украшение: каждая
включает свой пласт продукта, который иначе не запускается.

| Роль | Как пишет | Что этим проверяется | Отправка на Stepik |
|---|---|---|---|
| **1. Новичок** | ошибается по каталогу типов (ниже) — не тот тип, забытый `int()`, сдвиг индекса, лишний перевод строки | **ложный AC** — грейдер принял неверное; совпадение диагноза с `hint` платформы; диагностика падения, карточка глоссария при `RE`, «Подучить», AI-подсказки, веб-разделы про ошибки | **да** |
| **2. Средний** | не ошибается, но выбирает неподходящие методы: код верный и медленный | режимы 3/4 (сравнение и микробенч), раздел «Стиль», рекомендации, поведение на границе таймаута | **да** |
| **3. Эксперт** | решает чисто и идиоматично | **ложный WA** — грейдер завернул верное решение; сверка с эталоном | **да** |

**Отправляются все три, и роль 1 — не в последнюю очередь.** Первая редакция
протокола выводила её из отправки как «заведомо неверную, судить нечего». Это
ошибка рассуждения: именно неверные решения ловят **ложный AC** — самый тяжёлый
класс дефекта в проекте. Проверить его решениями эксперта нельзя в принципе:
там грейдер и платформа согласны по построению. Без отправки роли 1 половина
матрицы вердиктов остаётся незаполненной.

Вторая её ценность сохраняется полностью: падения роли 1 **наполняют базу
реальными ошибками**, на которых потом и проверяются «Подучить», карточки
диагностики и веб-разделы про ошибки. Сейчас у них на входе три записи фикстуры.

### Косячные ответы берутся из каталога, а не сочиняются

Ошибка роли 1 — не «какая-нибудь», а **классифицированная**: только так сверка
проверяет диагностику по каждому типу отдельно. Каталог уже есть —
`scripts/corpus_mutations.py`: `timeout`, `runtime_error`, `syntax_error`,
`no_output`, `extra_line`, `dropped_last_line`, `blank_line_append`,
`trailing_space`, `upper_case`, `vertical_tab`, `float_noise`, `crlf_newlines`.

Из этого следует **четвёртый источник решений**, которого не было в замысле:
**мутации эталона отправляются на Stepik тоже**. Сегодня каталог мутаций
проверяется против ожиданий, написанных человеком, — и первый же прогон корпуса
показал, что расхождение было ошибкой **в мутации**, а не в грейдере. Отправка
даёт этим ожиданиям внешнюю валидацию: платформа говорит, засчитывается
мутированное решение или нет, и гадать больше не о чем.

### Сверяется не только вердикт, но и диагноз

`SubmissionResult` несёт `hint` — сообщение проверяющей системы Stepik. Оно
сравнивается с тем, что сказал грейдер: совпал ли **номер упавшего теста**,
совпал ли тип падения. Это бьёт прямо в главный дефект аудита — формат 2 молча
выбрасывает кейс с ведущим нулём, из-за чего нумерация кейсов расходится. На
реальных задачах курса расхождение номеров и будет тем самым доказательством.

---

## Сбор и разбор разделены

**На шаге ничего не анализируется.** Шаг только собирает: прогнать, отправить,
записать сырое. Разбор — на границе **главы** и окончательно в облачном окне.

Почему именно так:

- **паттерн виден на главе, а не на шаге.** Одиночное расхождение вердиктов
  выглядит случайностью; три подряд на задачах одного формата — это класс
  дефекта с готовым репро;
- **разбор на месте утраивает время шага**, а прогон и без того длинный;
- **это и есть двухоконный контракт:** локальное окно собирает (там сеть и
  токен), облачное разбирает и чинит (там дешевле думать, там тесты и PR);
- **обрыв стоит одного шага, а не всей работы** — сырое пишется сразу.

Единственное решение, принимаемое **на шаге**, — «можно ли продолжать»: если
валится каждая отправка, платформа отвечает `429`, шаг закрыт по числу попыток
или протух токен, сбор останавливается. Это проверка живости, а не анализ находок.

**Контрольная точка — граница главы.** Там пишется короткая сводка (сколько
шагов, сколько расхождений, по каким классам), и там же можно прерваться и
вернуться позже: глава — естественная единица прогона.

## Процедура одного шага

```
1. Скачать задачу штатным загрузчиком (условие, tests/, meta.json)
2. Роль 1 (косяк по каталогу) → прогон локально → ОТПРАВКА → журнал (два вердикта + hint)
3. Роль 2 (верно, но медленно)  → прогон локально → ОТПРАВКА → журнал (два вердикта + hint)
4. Роль 3 (чисто)               → прогон локально → ОТПРАВКА → журнал (два вердикта + hint)
5. Эталонное решение (принятое ранее) → прогон локально → журнал
6. Мутации эталона по каталогу → прогон локально → ОТПРАВКА → журнал
7. Дописать журнал шага НА ДИСК и идти дальше — без разбора
```

Между отправками — пауза; при `429` уважается `Retry-After`.

## Что собирается

Артефакт главы — не только журнал:

| Что | Зачем |
|---|---|
| **журнал прогона** (запись на каждое решение) | основной материал разбора |
| **сами решения** трёх ролей и мутаций | без них расхождение невоспроизводимо |
| **срез базы истории** после главы | на нём проверяются «Прогресс», серия, бейджи |
| **логи грейдера** за прогон | там видно, что подсистема сказала до того, как это дошло до экрана |

### Запись журнала

На каждое решение — одна запись, **сырая, без выводов**:

```
шаг:              id, урок, курс, тема
роль:             1 | 2 | 3 | эталон | мутация:<ключ>
ожидалось:        для роли 1 и мутаций — какой вердикт закладывался
вердикт локально: AC | WA | RE | TL | ошибка формата
вердикт Stepik:   correct | wrong | evaluation | error | — (не отправлялось)
hint Stepik:      текст проверяющей системы дословно
диагноз грейдера: тип падения и номер упавшего кейса
что показал продукт: карточка глоссария, подсказка «Подучить», строка «Стиль»
время/память:     для роли 2 — попал ли в таймаут
```

Поле «что сломалось» здесь отсутствует намеренно: это уже вывод, а выводы
делаются на разборе. Журнал фиксирует наблюдения.

## Матрица вердиктов: ради чего всё затевалось

| Локально | Stepik | Что это значит |
|---|---|---|
| `AC` | `AC` | согласие — норма |
| `AC` | `WA`/`RE` | **ложный AC** — грейдер принял неверное решение. Самый тяжёлый класс дефекта в проекте |
| `WA`/`RE` | `AC` | **ложный WA** — грейдер завернул верное. Второй по тяжести |
| `WA` | `WA` | согласие; отдельно сверяется, **совпал ли диагноз** — тип падения важен не меньше вердикта |
| `RE` | `WA` | вердикт сошёлся, а тип падения разошёлся: диагностика вводит в заблуждение |

У роли 1 и мутаций есть **два** эталона сразу: внутренний (ошибка внесена
намеренно, ожидаемый вердикт пишется в журнал **до** прогона) и внешний
(вердикт платформы). Расхождение внутреннего с внешним — тоже находка, только
не про грейдер, а про сам каталог: значит ожидание в нём написано неверно.

Разбор идёт **по классам, а не по шагам**: находка — это повторяющийся класс с
двумя-тремя примерами, а не строка журнала.

---

## Границы

**Правило, которое держит всю затею в рамках: отправляем только туда, где шаг
уже засчитан.** Прогресса это не создаёт — он там давно стоит, — а значит
прогон не является накруткой ни по смыслу, ни по результату: состояние аккаунта
после него ровно то же, что и до. Отличие от ботоводства не в намерении, а в
проверяемом факте — новых зачётов не появляется.

- **Отправка — только по пройденным курсам** (`58852`, `68343`, `227004`,
  `174852`): перерешение собственных, уже сданных задач.
- **Курс `82541` (профессионалов) не пройден — там отправки нет.** Именно этот
  запрет и удерживает правило выше: отправка засчитала бы шаги, которых владелец
  не проходил, и прогресс стал бы результатом работы агента. Курс проверяется
  ровно так же, но локальным вердиктом, без внешнего судьи. Снять запрет может
  только владелец и только явно — по умолчанию он действует.
- **Темп отправки.** Плотный поток POST на `/api/attempts` — нагрузка на чужую
  платформу и риск для аккаунта. Между отправками — пауза, при `429` уважается
  `Retry-After`.
- **Чужой контент не коммитится:** условия и тесты курсов живут вне git.
  В репозиторий едут только производные данные — что и где разошлось.
- **Секреты не покидают локальную машину:** ни `secrets.json`, ни токен в
  журнале, ни в отчёте, ни в issue.

---

## Что нужно построить заранее (в облаке)

Инфраструктура почти вся есть — обход курса (`corpus_fetch.iter_course_steps`),
загрузчик, отправка (`core/stepik_client.submit_and_wait`), эталонное решение
(`core/stepik_reference.py`), грейдер, история. Не хватает трёх вещей, и они
делаются **до** прогона:

1. **Оркестратор шага** — связать перечисленное в последовательность выше, с
   дописыванием журнала после каждого шага и возобновлением с места обрыва.
2. **Журнал прогона** — формат выше, устойчивый к обрыву (дозапись, не
   перезапись), с ключом «шаг + роль».
3. **Дыры стенда из `local-sweep.md § Готовность к фазе L`** — группа `ai`,
   сценарий полного цикла, чтение настоящей базы истории.

Плюс то, что делает только человек или локальная сессия: **сами решения от трёх
ролей**. Их пишет агент локальной сессии по теме шага — это не генерация
скриптом, а работа по условию задачи.

---

## Объём и порядок

Курсы «Поколение Python» — сотни шагов с кодом, три решения на шаг плюс эталон.
Полный прогон по всем пяти курсам — работа не на один вечер, поэтому:

1. **Пилот на одной главе** пройденного курса: замерить время на шаг, поймать
   всё, что мешает автоматизации, отладить журнал.
2. **Курс для начинающих целиком** — самый ценный материал: там простые темы,
   а значит ошибки роли 1 чистые и хорошо ложатся в карточки.
3. **Остальные пройденные курсы** — по мере сил, начиная с продвинутого.
4. **Курс для профессионалов** — последним и без отправки.

Экономия ложная, если резать роли: две роли вместо трёх убирают ровно тот пласт,
ради которого прогон и затевается. Резать нужно **число шагов**, а не глубину
проверки одного шага.
