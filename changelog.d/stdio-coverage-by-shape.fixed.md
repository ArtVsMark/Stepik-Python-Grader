Защита вывода от `UnicodeEncodeError` в консоли cp1251/cp866 добавлена в 41 скрипт `scripts/`, который её не имел, а охват гарда теперь вычисляется признаком (запускается процессом и печатает не-ASCII), а не ручным списком: список не рос вместе с каталогом, и дефект вернулся в четвёртый раз — новый гейт покраснел сразу на трёх джобах Windows (#PR)
