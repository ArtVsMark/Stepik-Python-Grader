`gh_rest.py` закрыл последние операции, ради которых окно уходило в GraphQL: `auto-merge` (одна короткая мутация вместо MCP-операции в ~300 points), `sub-issues`/`add-sub-issue` по REST и стоп-кран по остатку квоты — ниже порога модуль предупреждает, а `rate` возвращает код «ждать» (#1282)
