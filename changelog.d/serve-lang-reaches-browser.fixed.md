`--serve --lang en` открывает английский интерфейс, а не только переводит сообщения API: стартовый язык страницы приходит от сервера, тогда как фронт брал его лишь из `localStorage` с откатом на `ru` — единственный явный выбор языка игнорировался (#1139)
