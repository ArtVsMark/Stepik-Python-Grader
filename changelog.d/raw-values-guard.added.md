В ответ веб-слоя уходит число, а не его вид: `check_raw_values.py` не даёт положить в поле отформатированную величину (`f"{x:.1f} с"`) — клиенту пришлось бы разбирать строку обратно вместе с единицей и округлением, которых он не выбирал (#1384)
