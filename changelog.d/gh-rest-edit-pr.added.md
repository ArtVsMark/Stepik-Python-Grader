`gh_rest.py edit-pr` правит заголовок и тело уже открытого PR: тело веток `agent/**` берётся из сообщения коммита один раз, и `Closes #N`, ставший известным позже, до сих пор оставалось только советовать проставить руками (#PR)
