Гейты `preflight.py` и `check_pr_ready.py` перестали падать в однобайтовой консоли: вывод переключается на UTF-8 до первой печати, включая `--help`. Заодно закрыта зеркальная половина — текстовый `subprocess` без явной кодировки: `check_pr_ready.py` так ронял свой вызов `gh api` на русских темах коммитов, молча уходил на анонимный REST и отвечал «rate limit exceeded» вместо готовности PR (#1243)
