Лимит памяти решения проверяется настоящим прогоном, а не только подделкой модуля `resource`: семнадцать тестов сверяли аргументы вызова `prlimit` и оставались зелёными на неработающем лимите. Три новых кейса задают тот вопрос, который видит студент, — жадное решение под лимитом не доезжает, скромное под тем же лимитом проходит, а без лимита доезжает и жадное (#1441)
