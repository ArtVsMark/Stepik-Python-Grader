Группа `ai` в прогонном стенде (`corpus_sweep.py --modules ai`): подсказка проверяется на настоящем падении реальной задачи — канал молчит без провайдера, заземление собрано и не шире `ai_grounding_k`, свой `ai_system_prompt` доезжает до запроса, отказ провайдера возвращается причиной. Транспорт подменяется заглушкой, живой провайдер и ключ не нужны ()
