preflight показывает карту «кто ещё трогает те же файлы» сам, неблокирующим шагом: прежде механизм был добровольным, и причиной значилось «список чужих веток стоит обращения к API» — скрипт читает локальные git-ссылки, без сети и токена (#1400)
