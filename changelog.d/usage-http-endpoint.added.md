Журнал прогонов доступен соседнему инструменту по HTTP: `GET /api/v1/usage` под `--serve --expose-usage` отдаёт те же события в схеме `stepik-grader/usage/1`, что и CLI-команда; без флага эндпоинт отвечает 404 — поднять сервер для себя и открыть накопленное соседу это разные решения (#1365)
