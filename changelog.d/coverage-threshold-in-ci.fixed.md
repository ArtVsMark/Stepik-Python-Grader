Прогон части тестов (`pytest tests/test_x.py`, `pytest -k ...`) больше не завершается кодом 1 при зелёных тестах: порог покрытия переехал из `pyproject.toml` в флаг `--cov-fail-under=85` шага CI, где прогон заведомо полный. В конфиге он применялся к любому запуску, потому что `--cov` включён в `addopts` безусловно, и читающий код возврата видел «тесты упали» на пустом месте (#PR)
