# WEB UI — замыслы, отложенное и отклонённое

> Design-only материал, выделенный из бывшего `web-mvp.md` (issue #267,
> разделение реализованного и замыслов). Всё, что реализовано в
> `src/stepik_grader/web/` — в [web-current.md](web-current.md), не здесь.
> Этот документ — не план на следующий спринт с конкретными датами, а
> накопительный список идей "later"/отклонённых решений, чтобы не терять
> контекст, зачем от чего-то отказались или что осталось не сделано.

## Оглавление

- [Экспорт/синхронизация глоссария во внешний проект](#экспортсинхронизация-глоссария-во-внешний-проект)
- [Deep-linking через URL-хэш](#deep-linking-через-url-хэш)
- [Action cards, отложенные](#action-cards-отложенные)
- [Command palette, отложенное](#command-palette-отложенное)
- [Per-block detail-разбор микробенча](#per-block-detail-разбор-микробенча)
- [Scenario buttons (отклонено)](#scenario-buttons-отклонено)
- [Полный a11y-аудит](#полный-a11y-аудит)
- [MVP vs v1 vs later — статус-трекер](#mvp-vs-v1-vs-later--статус-трекер)

---

## Экспорт/синхронизация глоссария во внешний проект

Готовые карточки (`status: ready`) из локальной базы (см.
[web-current.md § Глоссарий](web-current.md#глоссарий-как-локальный-knowledge-модуль))
могли бы экспортироваться/синхронизироваться в HTML-проект
[Glossary-Python](https://github.com/ArtVsMark/Glossary-Python)
(bridge/exporter). После экспорта карточка помечается `exported`. Экспорт
задуман **одно-направленным** (grader → Glossary-Python), внешний проект
остаётся независимым — двусторонняя синхронизация не планируется вовсе
(non-goal, не просто "later").

Соответствующий эндпоинт `POST /api/glossary/export` — не реализован (нет в
`web/server.py`), в отличие от остальных эндпоинтов `/api/glossary/*`, уже
описанных в [api.md](api.md) как рабочие. Трекается как #126-follow-up.

Adapters-слой для этого («Glossary exporter/bridge») тоже не реализован —
см. [web-current.md § Архитектура web UI](web-current.md#архитектура-web-ui)
для списка того, что реализовано вместо него.

## Deep-linking через URL-хэш

Задумывалось: состояние раздела/пути/карточки отражается в URL-хэше
(`#/check?path=…&mode=tests` / `#/glossary/recursionerror`), чтобы action
card «Open glossary» и история браузера работали как обычные ссылки.
**Частично реализовано:** deep-link карточки глоссария `#/glossary/<id>`
работает (issue #329) — сюда ведут error cards и мини-карточки «Функции в
коде». Общий deep-linking остального состояния (раздел + путь + режим, напр.
`#/check?path=…&mode=tests`) остаётся нереализованным.

## Action cards, отложенные

Из MVP-набора (реализован целиком, см.
[web-current.md § Action cards](web-current.md#action-cards)) осознанно
исключены:

| Действие | `id` | Контекст | Что должно делать |
|---|---|---|---|
| Create test | `create_test` | любой кейс | Заготовка нового тест-кейса (`N`/`N.clue`) из stdin/expected |
| Compare solutions | `compare_solutions` | режим bench / ≥2 решения | Открыть сравнение (таблица bench уже ранжирует по медиане) |

Backend-функция `grade_benchmark(reference=...)`/`_apply_reference_ranking`
(вердикты `REFERENCE`/`FASTER` поверх `SIMILAR`/`SLOWER`/`MUCH_SLOWER`,
`core/microbench_runner.apply_relative_ranking` не тронут) **уже есть в коде
и покрыта тестами**, но фронтенд её не вызывает ни в одном режиме —
пригодится, когда `compare_solutions`/эталонное решение из #55 понадобится
подключить к UI. Кнопка «Найти эталонное решение» в UI режима 1 существует,
но `disabled` — задел под #55 (переоткрыт, полуавтоматический импорт
закреплённого решения со Stepik JSON), не трогать до реализации #55.

## Command palette, отложенное

- Внешние плагины команд (сторонний код регистрирует свои записи в реестре)
  — не реализовано.
- Изначальный дизайн предполагал `when` как строку-предикат (произвольное
  выражение вида `"case.glossary_ids != []"`); по факту в
  `src/stepik_grader/web/commands.py` это фиксированный словарь тегов
  контекста — сознательное упрощение при реализации #125, не баг и не
  недоделка, задокументировано как окончательное решение в
  [web-current.md § Command palette](web-current.md#command-palette-ctrlk).
- Поиск в палитре — substring, не fuzzy-matching; апгрейд до fuzzy остаётся
  идеей "later".

## Per-block detail-разбор микробенча

Микро-бенчмарк (режим 4, см.
[web-current.md § Режимы проверки](web-current.md#режимы-проверки-и-микро-бенчмарк))
реализован целиком, но detail panel при клике по строке результата не
показывает разбор **какие именно блоки замерялись** (stdin-block через
`tracemalloc` vs function-block через RSS) — это осталось идеей "later", не
реализовано.

## Scenario buttons (отклонено)

Проблема, которую эта идея пыталась решить: «button soup» — набор из
десятка кнопок, большинство из которых неприменимы к текущему состоянию,
перегружает и путает. Предполагавшееся решение — **сценарные
(контекст-зависимые) кнопки**: auto-показ `run_again`/`toggle_theme`/
`switch_section`, когда ничего не выбрано, в зависимости от состояния:

| Состояние | Показываемые кнопки (задумывалось) |
|---|---|
| Пусто (ничего не проверено) | `Проверить`, `Открыть глоссарий` |
| Все AC | `Run again`, `Benchmark`, `Compare solutions` (если ≥2) |
| Есть WA | `Explain error`, `Copy input`, `Copy output`, `Run again` |
| Есть RE | `Explain error`, `Open glossary`, `Copy input`, `Run again` |
| Есть TLE | `Explain error`, `Run again`, `Create test` |
| Режим bench | `Compare solutions`, `Run again` |

**Статус: ❌ отклонено по фидбеку владельца** (issue #125, комментарий к
PR) — auto-показ этих кнопок был признан лишним и убран из приложения
целиком; command palette и action cards в detail panel закрывают тот же
сценарий без отдельного механизма. Не планируется к реализации — не
"later", а именно отказ от идеи. Сохранено здесь для истории решения, а не
как открытый пункт.

## Полный a11y-аудит

Базовый уровень доступности реализован (семантическая разметка, `role`/
`aria-label`, контраст, клавиатурная навигация) — см.
[web-current.md § Доступность](web-current.md#доступность-клавиатура-тёмная-тема).
Полный a11y-аудит (проверка screen reader'ами, WCAG-чеклист, тестирование с
реальными вспомогательными технологиями) остаётся не выполненным.

## MVP vs v1 vs later — статус-трекер

Канонический трекер статуса возможностей web-слоя. Обновляется по мере
реализации/отказа от идей (последнее обновление — issue #267).

| Возможность | Статус |
|---|---|
| `--serve`, таблицы tests/bench | ✅ реализовано |
| Error card RE + glossary-ссылка | ✅ реализовано (issue #72) |
| Split-pane workspace | ✅ реализовано (#125) |
| Error cards WA/TLE (расширенные поля) | ✅ реализовано (#125) |
| Downloader / загрузка тестов из Stepik | ✅ реализовано, отдельный раздел (#186) |
| — web-OAuth (браузерный редирект) | later, не планируется как приоритет |
| — batch-скачивание курса целиком | later |
| Микро-бенчмарк (режим 4) в web | ✅ реализовано (#187) |
| — per-block detail-разбор | later, см. выше |
| Action cards (copy/run/explain/open) | ✅ реализовано (#125) |
| — `create_test`, `compare_solutions` | later, см. выше |
| Command palette (Ctrl+K) | ✅ реализовано (#125, фикс. словарь тегов вместо predicate-строки, substring-поиск) |
| — внешние плагины команд, fuzzy-поиск | later |
| Scenario buttons | ❌ отклонено (issue #125 comment), не планируется |
| Раздел «Глоссарий» (поиск/карточки) | ✅ реализовано поверх store (#125) |
| Детектор недостающих концепций + очередь | ✅ web-интеграция готова (#125, backlog-view) |
| — полуавто-наполнение карточек | later |
| Экспорт/синхронизация в Glossary-Python | design, exporter #126-follow-up |
| — двусторонняя синхронизация | non-goal, не планируется вовсе |
| Тесты web MVP (download + microbench + glossary) | ✅ реализовано (#129) |
| Deep-linking через URL-хэш | частично: `#/glossary/<id>` реализован (#329); общий (раздел/путь/режим) — later |
| Полный a11y-аудит | later, см. выше |
| Выделенный бэкенд/SPA-фреймворк (FastAPI и т.п.) | не требуется; только по явному решению |

Контекст по каждому закрытому issue (#125/#186/#187/#129/#126) — в истории
PR и `CHANGELOG.md`; детали реализации — в [web-current.md](web-current.md).
