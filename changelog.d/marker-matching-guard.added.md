Константа-маркер больше не сверяется началом строки: `check_marker_matching.py` не даёт подставить её в `startswith`, потому что `<!-- ci-failures -->` совпал бы с `<!-- ci-failures-old -->`; заодно `_GENERATED_MARKER` переименован в `_GENERATED_PREFIX` — он хранил префикс, а имя называло его маркером (#1384)
