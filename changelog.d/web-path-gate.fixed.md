Две ручки веб-API перестали работать с чем угодно внутри workspace: `POST /api/save-solution` больше не затирает `secrets.json` текстом решения, а `POST /api/v1/hint` не увозит его содержимое внешнему AI-провайдеру — обе проверяют, что путь ведёт к файлу решения (`.py`), как это с #811 делает показ исходника. Конфайн отвечает на вопрос «внутри ли workspace», но не на вопрос «это вообще решение»; гейт вынесен в общий метод, чтобы третья такая ручка не появилась без него (#1121)
