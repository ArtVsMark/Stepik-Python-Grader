Связь «задача → правило каталога» перестала быть односторонней: `scripts/link_rules_to_issues.py` ставит на задачу-источник метку `породило правило` и оставляет ОДИН комментарий со ссылками, обновляемый по месту — идемпотентность держится скрытым маркером в теле, а не совпадением текста. Пишет только по `--apply`; зовёт ночной прогон `tracker-guardrails.yml` (#1351)
