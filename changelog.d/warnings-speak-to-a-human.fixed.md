Предупреждения о неполном наборе тестов печатаются человеку, а не как трассировка Python. Вместо четырёх строк с путём в `src/`, номером строки и куском нашего кода — одна строка «⚠️ <текст>», в том же тоне, что и остальные сообщения. Машиночитаемый ключ `warnings` в `--output json` не изменился: `warnings` остаётся транспортом между ядром и CLI, менялся только показ (issue #1466).
