Окна «Согласие на AI» и «Обратная связь» перешли на общий помощник модалок из `core.js`: после клика по затемнённой подложке `Escape` снова закрывает окно, а `Tab` держится внутри диалога и не уходит на страницу под ним. Прежде каждое окно несло свою копию focus-trap со слушателем на самом оверлее — клик по подложке уводил фокус на `body`, событие клавиатуры до оверлея не доходило вовсе, и окно переставало слушаться клавиатуры, хотя мышью всё работало (#1225)
