Лаунчер открывается в корне проекта, а не в папке, откуда его позвали: рабочая папка по умолчанию берётся из общего `workspace_root()` — прежний фолбэк на cwd означал промах периметра `--root` и 403 на задачах у всех, кто собрал папку задач руками, а не загрузчиком (#1138)
