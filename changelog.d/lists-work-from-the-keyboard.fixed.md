Списки выбора работают с клавиатуры. Список решений, карточки глоссария и правила рисовались как `<li>` с обработчиком клика — ни `tabindex`, ни роли, ни клавиш: до выбора решения нельзя было добраться вообще, а значит и до проверки. Теперь это listbox по WAI-ARIA: стрелки, Home/End, Enter/Пробел, roving tabindex (весь глоссарий из тысячи с лишним карточек — одна остановка Tab, а не тысяча) и возврат фокуса после перерисовки. Механизм один на все три списка (issue #922, находки `FE-2-08` и `FE-3-02`).
