<!-- СГЕНЕРИРОВАНО scripts/generate_rules_digest.py — не править руками -->

# Правила: утверждения одной строкой

> **Второй рубеж.** Первый — механизм: гейт краснеет, и правило действует
> независимо от памяти окна. Но механизмом покрыто не всё, и остальное
> держится тем, что окно про правило знает. Здесь оно и написано —
> утверждение каждого правила одной строкой, чтобы это можно было прочесть
> на старте, а не когда правило уже нарушено.
>
> Порядок групп не алфавитный и не по номеру: **сначала то, что не поймает
> машина**. Правило с гейтом окну помнить не обязательно.
>
> Полный текст правила — в каталоге, файл `rules/ru/<номер>-*.md`; чем оно
> держится здесь — в [`.rules/bindings.json`](../../../.rules/bindings.json).

Правил в каталоге: **147**, окну важны **101** — не держится ничем — только вниманием окна: 55, ответа по правилу ещё нет: 0, держится шагом процесса: 6, держится гейтом: 40.

Остальные — `not-applicable` и `rejected`: предмета здесь нет либо решение
иное, и ответ записан в `.rules/bindings.json` вместе с причиной.

## Не держится ничем — только вниманием окна — 55

Механизма нет. Если окно забудет, не заметит никто: ни CI, ни ревью.

- **006** Агентское окно живёт три–пять дней. Долгоживущая сессия дорожает и консервирует устаревшие правила. Дальше срока — перезапуск с передачей эстафеты.
- **011** Наблюдение: событие вместо опроса, а если опрос — то условный. Цикл наблюдения — самый дорогой режим работы с API. Подписка предпочтительнее опроса; опрос — только условный и только по дельте.
- **014** «Тест краснеет до фикса» доказывается полу-откатом, а не откатом всего. Убирайте только поведение, оставляя новые имена. Иначе доказано не то, что вы думаете.
- **015** Агенты возвращают данные — файлы правит хост. Параллельные исполнители отдают результат, а не пишут в общее дерево. Запись — за одним.
- **016** Обрезать вывод молча нельзя — только с маркером обрыва. Урезанный результат обязан сообщать, что он урезан, и насколько. Иначе он выглядит полным.
- **017** Остаток лимита мерить, а не угадывать — и смотреть первым шагом. Диагностика начинается с фактов о квоте, а не с гипотез о причинах. Запрос остатка обычно бесплатен.
- **018** Одно окружение проверяет узлы, другое — цепочку. У каждой проверки есть окружение, где она осмысленна. Проверка не в том окружении даёт ложный зелёный.
- **019** Аудит планируется от поверхностей продукта, а не от файлов. Обход по файлам даёт покрытие кода и пропускает то, чего в коде нет. Начинайте со списка того, с чем сталкивается пользователь.
- **020** После сбоя перезапускать дельту, а не всю волну. Разберите, что уже сделано, и повторите только недостающее. Полный перезапуск платит дважды за один результат.
- **027** Пустое состояние надо объявлять явно. «Здесь сейчас пусто, дата» — это информация. Просто пустой файл — неоднозначность.
- **031** Параллельные исполнители запускаются волнами фиксированного размера. Не залпом. Волна, разбор, следующая волна. Большой залп сам себе создаёт отказ, и чем он больше, тем дороже потеря.
- **032** Если предмет роли наблюдаем в работающем продукте — роль обязана его запустить. Вывод, сделанный чтением кода там, где можно было запустить, считается неполным.
- **033** Темп длинной работы считается от лимита, а не от желания. Интервал между запусками выводится арифметически из бюджета. Иначе работа упрётся в стену посреди волны — и потеряет всю волну.
- **034** Зона одного исполнителя должна быть маленькой. Роль дробится на двух-трёх исполнителей, плану даётся не больше трёх пунктов. Большая зона гибнет целиком.
- **036** Дорогое окружение входит в аудит дважды и коротко. Аудит строится тремя фазами: короткий слепок реальности **до** массовой работы, вся массовая работа в дешёвом окружении, короткое подтверждение по накопленному чек-листу — **в конце**. Дорогое …
- **037** Находка, полученная не на той поверхности, — гипотеза. Дефект, найденный на подделке, но живущий на настоящей поверхности, не получает тяжести до подтверждения на настоящей. Он идёт в чек-лист другого окружения со статусом «гипотеза».
- **038** Имя окна начинается с окружения, а не с задачи. Первое, что стоит в названии агентской сессии, — метка окружения: `[WEB]`, `[LOCAL]` или `[CLI]`. Задача идёт после. Имя присваивается при открытии, а не когда понадобилось.
- **039** У проверки три исхода, а не два. «Чисто», «нашли проблему» и **«проверка не отработала»** — три разных исхода с тремя разными действиями. Третий различается по наличию результата, а не по коду возврата.
- **042** Решение записывается вместе с отвергнутыми вариантами. Запись о решении состоит из контекста, самого решения, **рассмотренных альтернатив** и последствий. Без альтернатив это не решение, а объявление.
- **043** Решение не правится задним числом — его отменяет новое. Изменение решения — **новая** запись, которая помечает старую заменённой. Старую не редактируют.
- **047** Смена правил работы — повод перезапустить окна, а не рассылка. Изменили правила, по которым работают окна, — перезапускаете окна. Возраст окна тут ни при чём: правило меняется событием, а контекст читается один раз, при старте.
- **054** Сбор и разбор — разные проходы. На шаге сбора ничего не анализируется: прогнать, записать сырое, идти дальше. Разбор — отдельным проходом, на границе естественной единицы работы.
- **055** Собственный эталон — тоже гипотеза. Ожидание, написанное своей рукой, не доказывает ничего. Пока его не подтвердил внешний источник, расхождение с ним означает «один из двух неправ», а не «продукт сломан».
- **057** Правило, которое нельзя проверить машиной, называется явно. Если механизма для правила не существует, оно не выбрасывается и не считается очевидным — оно записывается отдельным разделом с пометкой, что гейта нет и почему.
- **058** Исчерпав квоту — остановиться, а не повторять. За нулём счётчик продолжает расти: повтор не «пробует ещё раз», он отодвигает сброс. И хуже расхода — полусделанные состояния, которые остаются после отбитых вызовов.
- **059** У каждого исчерпаемого ресурса есть заранее составленная карта обхода. Заранее известно, что перестаёт работать и что продолжает работать при исчерпании каждого ресурса. Режим при исчерпании — **копить**, а не ждать.
- **060** Разбор после каждой волны, и качество важнее механики. Следующая волна запускается только после разбора предыдущей, по двум осям: кто упал — и **что вышло**. Вторая ось важнее первой.
- **061** Запреты окружения пишутся в задании, а не подразумеваются. Исполнитель, который будет запускать продукт, получает список запретов **в тексте задания**. Он не видит того, что знает хост, и упрётся в защиту, не понимая, во что упёрся.
- **062** Роль заводится, если способна возразить, а не дополнить. Новая роль проходит приёмку: свой вопрос, свой артефакт, **своё возражение** конкретной существующей роли. Не проходит — это раздел документации, а не роль.
- **063** Автоматическое вмешательство включается по всем условиям сразу. Если ложное срабатывание портит чужую работу, критерий пишется конъюнкцией: вмешиваться, только когда выполнены **все** условия. Любое невыполненное — «не трогать».
- **082** Состав ролей покрывает все пласты продукта, а не только разработку. Направлений столько, сколько у продукта пластов. У каждого пласта есть владелец; пласт без владельца — не мелочь, а слепая зона. При этом покрытие **не равно** отдельной роли: направление может быть …
- **086** Тяжесть находки ставит не тот, кто её нашёл — но опровергателю нужна шкала. Оценку находке ставит отдельный проверяющий с установкой «опровергай по умолчанию». И его шкала калибруется примерами, иначе он не отсеивает слабое, а понижает всё подряд.
- **087** Повторный проход получает на вход прошлые находки и запрет их переоткрывать. Второй аудит по тому же предмету начинается с файла находок первого и с правила новизны: уже найденное находкой не считается — его можно только углубить или отбросить.
- **088** Критик проверяет метод фазы, а не предмет работы. В конце каждой фазы отдельный проверяющий отвечает не «что не так с продуктом», а «что не так с тем, как мы его проверяли»: какая доля результата действительно новая, что фаза не покрыла, где …
- **092** Находки и порядок разбора — разные документы. «Что не так и где» и «что за чем и почему» — два разных вопроса и два разных документа. Один крупный аудит рождает оба, и смешивать их нельзя.
- **097** У проверяющего инструмента две ошибки, и каждая держится своим тестом. Ложное «прошло» и ложное «не прошло» — разные дефекты с разными последствиями. Регрессия заводится на **обе** стороны отдельно; покрытие одной создаёт иллюзию покрытия обеих.
- **098** Единица дробления определяется употреблением, а не формальным признаком. Что считать одной сущностью, решает то, **как её используют**, а не формальный критерий вроде «один вызов — одна запись». Техническая проблема разбиения лечится метаданными, а не дроблением.
- **099** Конфликт классификации разрешается по последствию, а не по правильности. Когда сущность подходит сразу двум разделам, побеждает тот, чьё отсутствие оставит раздел пустым или бессмысленным. Спор о «правильном» месте не имеет решения; спор о последствии имеет.
- **100** Дедлайнов два: на запуск и на работу. Ограничение времени на выполнение не покрывает **старт**. Зависнуть можно до первой строки, и там нужен свой, короткий дедлайн. Поднимать его разрешено локально; умолчание остаётся коротким.
- **101** Повторяют только те отказы, которые могут пройти сами. Повтор имеет смысл для временных отказов — перегрузки, сбоя связи, недоступности. Постоянный отказ повтором не лечится: «не найдено» не станет найденным со второй попытки. И если сервер назвал паузу …
- **102** Снисхождение перечисляется таблицей и отключается режимом. Каждое послабление при сравнении названо явно, с причиной, и существует строгий режим, где не прощается ничто. Неперечисленное снисхождение превращается в набор эвристик, которому никто не доверяет.
- **106** Огласка умножает и хорошее, и плохое — сначала настоящий прогон. Продукт не показывают широко, пока он не прошёл **настоящий** сценарий целиком. Реклама не создаёт качество, она увеличивает и его, и его отсутствие.
- **111** Если инструмент может сделать сам — он делает, а не советует. Подсказка «наберите такую-то команду» уместна, только когда инструмент действительно не может выполнить её сам. И проверяется, есть ли у пользователя **канал**, куда эта подсказка придёт.
- **113** Контракт описывает правила собственной эволюции. Перечислить поля мало. Контракт обязан сказать, **что в нём стабильно, что расширяемо и как добавляют новое** — иначе каждое изменение превращается в переговоры с нуля, а потребители расходятся.
- **115** У настроек один якорь и ограниченная зона поиска. Поиск файла настроек вверх по дереву останавливается на границе проекта, а не на корне файловой системы. И якорь у всех поверхностей продукта **один** — иначе один запуск читает настройки из двух …
- **116** Сборщик результатов — тоже источник потерь, и у него своя сверка. Скрипт, собирающий результаты параллельной работы, проверяется наравне с самой работой. Обязательная сверка — **количество на входе против количества на выходе**; расхождение останавливает сборку.
- **117** У задания исполнителя есть числовые границы. В задание пишутся числа: предел размера каждого поля ответа, бюджет обращений к инструментам, список типов файлов, которые открывать нельзя. Без чисел исполнитель выбирает их сам — и выбирает плохо.
- **121** Закрытие контейнера — не доказательство закрытия работы. Эпик, веха, спринт закрываются по своим критериям, единицы работы — по своим. Завершение доказывается **счётчиком незакрытых единиц**, а не тем, что контейнер выглядит пустым.
- **122** Рядом с отформатированным значением отдают сырое. Форматирование — операция с потерей. Отдавая величину для показа, отдавайте рядом исходное число: разбор строки обратно — это восстановление того, что вы сами и уничтожили.
- **137** Сторож смотрит на сырое значение: форматирование идёт после него. Проверка «источник ответил» ставится на **сырое** значение. Отформатированное значение сторожу не показывают: форматирование теряет ровно тот признак, по которому сторож узнаёт пустоту, и превращает …
- **141** Маркер сверяется целиком, а не началом. Структурный маркер — заголовок, имя метки, ключ, префикс ветки — сверяется **целой строкой**, а не её началом. Сравнение началом молчаливо засчитывает соседний маркер, который с проверяемого …
- **142** У красного по расписанию должен быть адресат, иначе его не читает никто. Проверка, запускаемая по расписанию, обязана при отказе **дойти до человека** — задачей, сообщением, чем угодно адресным. Красное на вкладке прогонов адресатом не является. Пока адресата нет, такая …
- **145** Механизм, объявивший несколько исходов, прогоняется по каждому. Механизм, у которого объявлено больше одного исхода, проверяется прогоном **каждого** из них, а не только успешного. Прогон одного пути подтверждает, что механизм запускается, — и ничего больше. …
- **146** Зелёный гейт подтверждает себя, а не утверждение, вокруг которого построен. Гейт с двусторонним набором, прошедший мутации, подтверждает одно: механизм работает так, как объявлен. **Истинность утверждения, ради которого он построен, он не подтверждает.** Утверждение …
- **147** У переключателя, отменяющего операцию, должен быть адресат отмены. Переключатель, который по несовпадению отменяет операцию, отменяет её **молча**: «не совпало» и «совпало, но делать нечего» дают один наблюдаемый исход — ничего. Толкнувший видит успех своей команды …

## Ответа по правилу ещё нет — 0

Проект не решил, действует ли правило здесь. До решения — читать как действующее.

_Сейчас пусто._

## Держится шагом процесса — 6

Проверяется человеком в названный момент — то есть тоже вниманием, но с местом и временем.

- **003** Имя ветки может быть переключателем поведения, а не соглашением о стиле. Если конвейер реагирует на префикс ветки, это надо писать в правилах первой строкой — и объяснять, что случится при другом имени.
- **007** Окно, зависшее на разрешении, снаружи неотличимо от работающего. Прежде чем счесть работу брошенной, проверьте реестр сессий: окно может ждать разрешения, и тогда нужен человек, а не ремонт.
- **071** Намеренный дубль подписывается. Повтор, оставленный сознательно, помечается прямо в коде: с кем дублируется и **чем оплачено** объединение. Неподписанный дубль будет «исправлен» следующим же наведением порядка.
- **136** Вердикт по правилу выносится после перечисления всех его предметов, а не первого. Проект, отвечающий по внешнему правилу, сначала перечисляет **все** свои предметы этого правила и только потом пишет вердикт. Отклонение, вынесенное по первому пришедшему в голову примеру, отклоняет …
- **138** Эстафета собирается по ходу, а не в конце. Решение, принятое в разговоре, оседает артефактом **сразу** — задачей, комментарием, записью решения. Тогда передача сводится к ссылкам. Оставленное «на потом» существует только в контексте окна и …
- **139** Механизм подтверждается прогоном, а не чтением. Пока механизм не отработал на живом предмете, он не подтверждён — как бы правильно ни читался. Отказ площадки зависит от состояния, которого в коде нет, поэтому «прочитал и убедился» подтверждает …

## Держится гейтом — 40

Забудете — покраснеет CI или preflight. Здесь только заголовки: помнить их наизусть незачем.

- **001** Транспорт к GitHub: REST по умолчанию, GraphQL только там, где REST не умеет.
- **002** Правило без механизма — обещание, а не гарантия.
- **004** Конфликт — штатная ситуация конвейера, а не авария.
- **010** Пустой список проверок означает «не стартовало», а не «всё хорошо».
- **012** В чужую ветку не пушить.
- **013** Код с экранированием писать файлом, а не heredoc'ом.
- **040** Пропуск без причины неотличим от забытого теста.
- **051** Предупреждают о вероятном, запрещают достоверное.
- **053** Порядок очереди задаётся правилом, а не готовностью.
- **066** Блокировку берут на спутника, а не на файл, который заменяется целиком.
- **067** Уборка после сбоя не превращает сбой в успех.
- **068** Список разрешённого, а не список запрещённого.
- **069** Пишем поле, а не снимок, если писателей несколько.
- **070** Эвристическая защита ослабляется осознанно — с записью остаточного риска.
- **072** Причину ловит гейт, факт — фикстура: нужны обе.
- **073** Версия инструмента — из одного источника и с верхней границей.
- **074** Необратимый шаг проверяется инвариантами заранее.
- **075** Гейт, не нашедший предмета проверки, обязан упасть.
- **076** Сообщение ссылается на то, что есть у получателя.
- **077** Совпадение ключей — ещё не перевод.
- **078** Отмена — отдельный исход, а не разновидность ошибки.
- **081** Чужой код запускают из приватного каталога, а не из общего временного.
- **089** Из оригинала в его копию не ссылаются.
- **103** Сторож побочных эффектов обвиняет не виновника — и исключения задаются формой.
- **104** У событийной автоматики должна быть ручная кнопка.
- **109** Каждый выход из переходного состояния обязан быть терминальным.
- **118** Исходник хранится рядом с производным.
- **119** Свои артефакты держат вне маски входа.
- **120** Каталог правил ведётся по своим правилам, а указатель к нему генерируется.
- **124** Перезапускать минимум, но зелёное со второго раза — находка, а не починка.
- **126** У заморозки должен быть выход, не проходящий через замороженное действие.
- **128** Обязательное поле проверяется на полноту, а не на непустоту.
- **129** У каталога есть контракт потребления, а не только правила ведения.
- **131** Из облачного окна не пишут: на записи учётные данные подменяются.
- **132** Изменение везут одной темой: сборное честно, но неразбираемо.
- **133** Границу изменения задаёт пересечение файлов, а не число задач.
- **135** Чьим именем подпишется запись — свойство конкретного окна, и узнаётся оно записью.
- **140** Гейт проверяется тем, что он обязан отвергнуть.
- **144** Окно контекста для разбора прозы — абзац, а не предложение.
- **148** Запись изменения — кэш, а не состояние.
