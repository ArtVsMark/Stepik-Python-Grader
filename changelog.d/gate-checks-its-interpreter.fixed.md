Гейты и транспорт к GitHub отказываются работать под интерпретатором ниже `requires-python`, называя обе версии и способ починки: прежде `preflight.py` падал под 3.11 `SyntaxError`, а `gh_rest.py` и `check_pr_ready.py` работали молча — «пока везёт» и «работает» снаружи неотличимы (#1507)
