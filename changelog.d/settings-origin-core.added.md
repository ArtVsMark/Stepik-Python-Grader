Настройки прогона (таймаут, лимит памяти, строгость сравнения, пороги «Подучить», квоты песочницы и другие) теперь можно задавать в `.grader_settings.json` — у установки через pipx `pyproject.toml` нет вовсе, и до этого они были ей недоступны. Значение знает своё происхождение («по умолчанию» / «из pyproject.toml» / «изменено вами») и сбрасывается к унаследованному; список настроек закрытый, тюнинг остаётся в конфиге проекта (#1136)
