Очередь мержа теперь двигается сама: workflow `merge-queue.yml` по завершении зелёного прогона `main` обновляет ветку первого в очереди — голову считает тот же `gh_rest.py queue`, что и человек в терминале, поэтому порядок описан один раз. Прежде между «защита требует актуальной ветки» и «кто-то её обеспечивает» был зазор: кнопка Update branch ручная, `auto-merge` ветку не обновляет, а merge queue личному аккаунту недоступна — PR с 16/16 зелёных стоял `behind` и не двигался никогда. Обновление идёт отдельным токеном: пуш от `GITHUB_TOKEN` не запускает прогон на PR, и голова застряла бы без единой красной проверки; гейт `check_workflow_guardrails.py` эту подмену не пропустит (#PR)
