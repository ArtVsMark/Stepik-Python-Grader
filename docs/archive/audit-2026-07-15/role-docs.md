## 📝 Документационный аналитик

### Методика

Проверено по коду ветки `claude/blissful-noether-aw7qjt` (v1.8.0 + 32 коммита, включая web-UX эпик #362/#364–#370, разделы «Правила/Подучить» #342/#348 и глоссарные волны #388/#390). Что сделано: (1) построчная сверка `docs/api.md` с маршрутизацией `web/server.py` (все GET/POST-ветки), `docs/configuration.md` с `config.py::GraderConfig` (18 полей), `docs/grader-workflow.md` с `cli/options.py` (флаги, дефолты `--repeats 15`/`--number 1000`, меню 0–5 из `core/locales/ru.json`); (2) AST-скрипт docstring-покрытия публичного API (318 публичных def/class); (3) скрипт проверки относительных ссылок по всем 25 md-файлам (0 битых); (4) пересчёт карточек глоссария по `glossary/data/*.json` (601 ready + 787 draft = 1388); (5) ревизия `docs/adr/`, `CHECKPOINT.md`, `docs/history.md`, `CHANGELOG.md [Unreleased]`; (6) верификация находок прошлого аудита (`docs/audit-2026-07.md` § 7) на актуальность.

### Сильные стороны

- **Дрейф из прошлого аудита реально закрыт (#353).** Все 10 пунктов § 7 аудита v1.7.0 проверены: инвариант CLAUDE.md про sandbox исправлен («Sandbox — только opt-in»); `docs/project-structure.md`/`architecture.md` знают не только `diag_log`/`tracer`/`playground`, но и все модули v1.8+ (`core/history.py`, `insights.py`, `lint.py`, `mtime_cache.py`, `rules/`, `web/rules_adapter.py`, `web/insights_adapter.py`); противоречие api.md про cancel устранено — текст (200 для завершённой job, 404 только при отсутствии/TTL) точно совпадает с `web/server.py:578-588`.
- **api.md — эталонная синхронность.** Все 17 эндпоинтов кода задокументированы, включая свежие `GET /api/rules`, `/api/rules/<code>`, `/api/insights` и параметр `?lang=` (#363) с fallback-семантикой. configuration.md покрывает все 18 полей `GraderConfig` вплоть до добавленных на этой неделе `record_history`/`insights_window_n`/`insights_active_threshold_t`/`insights_clean_streak_k`.
- **CI-guardrails документации** (`scripts/check_docs_guardrails.py`): бюджет README (220 строк, факт 131), проверка относительных ссылок **с якорями**, полнота индекса `docs/README.md`, лимит ≤3 версий в CHANGELOG с ротацией в `docs/changelog-archive.md` (#373). Это редкий для проекта такого размера уровень машинной защиты доков.
- **CHECKPOINT.md и docs/history.md актуальны** (snapshot v1.8.0, запись релиза `history.md:367`, таблица метрик доведена до v1.8.0), оба честно маркированы «исторический snapshot, не канон». `CHANGELOG.md [Unreleased]` ведётся в каждом PR — записи #344–#349, #363–#371 на месте.
- **Утверждение аудит-шаблона «ADR не заполнены» — ложно.** `docs/adr/0001-server-mode.md` — полноценный Nygard-ADR на 93 строки (Context/Decision/4 альтернативы/Consequences/Migration), плюс индекс `adr/README.md` с соглашениями.
- Onboarding-цепочка работает: README (131 строка, витрина) → quick start pipx → «Первый пример за 2 минуты» в grader-workflow.md → CONTRIBUTING § Быстрый старт; правило «канонический источник + ссылки» из `docs/README.md` § Канонические источники реально соблюдается (дублей не нашёл).

### Находки

1. **[medium] Хардкод-счётчики глоссария дрейфуют.** `README.md:44` — «581 карточка», фактически **601 ready** (после волны B1, PR #390; пересчитано по `glossary/data/*.json`). `docs/README.md:33` — «832 draft-карточек», фактически **787**. `docs/glossary.md:77` обновлён («601 на момент правки»), но остальные счётчики никто не сторожит — при 5 оставшихся волнах эпика #363 (B2–B6) числа будут протухать каждые 1–2 недели. Нужна либо нейтральная формулировка («600+ карточек»), либо guard в `check_docs_guardrails.py`.
2. **[medium] ADR-практика остановилась на первом ADR.** ADR-0001 висит в статусе `Proposed` с 2026-07-08, хотя фаза 1 его же плана миграции (Runner/#140) реализована, а направление де-факто принято и цитируется как канон — по собственным соглашениям `adr/README.md:19` статус пора двигать в `Accepted`. С тех пор приняты решения ADR-калибра без записи: выбор трёх sandbox-backend'ов (#266), схема SQLite-истории v1 (#344, `core/history.py`), динамическая версия через setuptools-scm (#162/#183), консолидация i18n/глоссария (#355/#356). «Почему» этих решений живёт только в закрытых issue/PR.
3. **[medium] Нет машинной сверки api.md ↔ роутер** — долг из прошлого аудита (§ 7 «Долг»), не закрыт: `openapi.yaml` отсутствует (`find -iname "*openapi*"` — пусто), контрактного теста «каждый маршрут `server.py` задокументирован и наоборот» нет. Сейчас api.md идеален, но синхронность держится на дисциплине; поверхность выросла до 17 эндпоинтов и растёт (rules/insights добавлены за неделю).
4. **[low] Ссылки на закрытый #351 как на трекинг.** `CLAUDE.md` (инвариант 4) и `README.md:113-115` пишут «в web-слой пока не проброшена (issue #351)» — #351 закрыт (фикс: `parser.error` при `--sandbox --serve`, `cli/__init__.py:461-468`). Факт «не проброшена» верен, но открытого issue на сам проброс SandboxRunner в `--serve` не существует — указатель ведёт в закрытую задачу и маскирует отсутствие трекинга.
5. **[low] Мелкий дрейф индекса и план-дока.** `docs/README.md:11` — «режимы 1–4», фактическое меню `[0-5]` с пунктом «5. Подучить» (`core/locales/ru.json:58`, `grader-workflow.md:41-48` уже обновлён). `docs/web-glossary-optimization-2026-07.md` — snapshot-план без статус-штампа реализации, хотя W1–W7 (#364–#370) и волна B1 уже влиты; прецедент правильного оформления есть — шапка «Статус реализации» в `audit-2026-07.md:10-15`.
6. **[low] Docstring-покрытие публичного API — 83%** (265/318). Большинство пропусков легитимны (визиторы `glossary/detector.py`, протокольные `run()` backend'ов, rich-fallback-стабы `reporter.py:54-64`), но есть настоящие дыры: статистические свойства `core/microbench_runner.py:86-98` (`min_time`/`median_time`/`mean_time`/`max_time`), `core/test_loader.py:51-81` (`TestCase`, `find_all_solution_files`, `collect_grouped_files`), `core/runner.py:227`. Требование CLAUDE.md «docstrings для всех публичных функций» ничем не проверяется.
7. **[info] MkDocs — пока оверкилл.** 21 док в `docs/` (~7 000 строк) + 2 ADR отлично читаются в GitHub-рендере; целостность ссылок и индекса уже сторожит CI; версионированные доки не нужны (инструмент локальный, релизы частые). Затраты на генератор (тема, деплой, CI-job, ещё один источник дрейфа навигации) сейчас не окупаются. Пересмотреть при появлении server mode/публичного API (#151) — тогда OpenAPI + сайт станут оправданы одновременно.

### Рекомендации

1. Убрать хардкод-числа карточек из README/docs/README (формулировка «600+») **или** добавить в `check_docs_guardrails.py` сверку счётчика с фактом по `glossary/data/*.json` — второе предпочтительнее, паттерн guard'ов в проекте уже отработан.
2. Реанимировать ADR: перевести 0001 в `Accepted`, дописать короткие ADR задним числом для sandbox-backend'ов и SQLite-схемы (они определяют будущее server mode #151/#153–#154 — именно для этого журнал и заводился).
3. Закрыть долг сверки API контрактным тестом (дешёвый вариант: тест парсит оглавление api.md и сравнивает с маршрутами `server.py`; OpenAPI отложить до server mode).
4. Пройтись по «протухшим указателям»: #351 в CLAUDE.md/README, «режимы 1–4» в docs/README.md:11, статус-штамп в web-glossary-optimization-2026-07.md — одним PR типа `docs: refresh stale pointers`.
5. Docstring-дыры добить точечно и рассмотреть `ruff` правило `D1` (pydocstyle) с per-file-ignores для визиторов — тогда инвариант CLAUDE.md станет машинным.

**Оценка направления: 9/10.** Документация — сильнейшая сторона проекта: канон против дублей работает, CI-guardrails уникальны для проекта такого масштаба, дрейф после бурной недели разработки — точечный и косметический. Балл снят за неподкреплённые машиной зоны (счётчики контента, api.md, docstrings) и застой ADR-журнала.
