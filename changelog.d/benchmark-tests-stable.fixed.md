Тесты относительной скорости перестали флейкуть: разрыв между «быстрым» и «медленным» решением поднят с 0.05 до 0.3 с — прежний тонул в запуске интерпретатора, и на занятой машине `SLOWER` превращался в `SIMILAR` даже при последовательном прогоне. Полный набор параллельно теперь зелёный целиком (#1249)
