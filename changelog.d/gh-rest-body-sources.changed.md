Тело сообщения во всех подкомандах `gh_rest.py` передаётся одинаково: `--body` либо `--body-file` (`-` — со стандартного ввода, UTF-8 без оглядки на кодовую страницу консоли); заодно `create-pr` принимает `--label`, а `close-issue` — `--comment` (#1286)
