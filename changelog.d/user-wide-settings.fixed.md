Настройки переехали в общий `~/.stepik-grader/settings.json` — туда же, где единая база истории: папочный тумблер записи управлял глобальной базой, поэтому включение в каталоге одной задачи не действовало из соседней, а выключение не выключало запись из другой папки. Существующий `.grader_settings.json` продолжает выигрывать (миграции нет), путь перекрывается переменной `STEPIK_GRADER_SETTINGS` (#1186)
