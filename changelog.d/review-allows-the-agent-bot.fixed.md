Авто-ревью перестало краснеть на каждом PR: событие `synchronize` шлёт тот, кто толкнул в ветку, а обновление ветки из `main` обязательно («Require branches to be up to date») и приходит от агентской сессии — ревью отказывалось работать с «Workflow initiated by non-human actor» и проверяло состояние ДО обновления. В `claude-code-review.yml` разрешён один известный бот поимённо, а не `*`; периметр прежний — job read-only, форковые PR пропускаются, запуск по упоминанию сохраняет гейт по `author_association`. Гейт `tests/test_claude_workflow.py` роняет прогон и на снятии строки, и на расширении её до `*` (#PR)
