Загрузчик задач говорит, что не так с `secrets.json`: называет файл, показывает абсолютный путь, по которому искали, и различает «файла нет», «файл не читается» и «кредов неполно» — прежде на все три случая была одна безымянная развилка. `GET /api/auth/status` отдаёт для этого `secrets_state` и `secrets_path_resolved`; содержимого файла в ответе по-прежнему нет (#1213)
