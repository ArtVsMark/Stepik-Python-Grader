Второй рубеж правил: дайджест `docs/agent/rules/DIGEST.md` — утверждение каждого правила одной строкой, сначала те, что не поймает ни один гейт, — и хук `SessionStart`, который кладёт его в стартовый контекст окна; расхождение дайджеста с `.rules/bindings.json` ловит `check_rules_digest.py` (#1384)
