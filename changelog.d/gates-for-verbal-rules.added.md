Пять правил, державшихся на словах, получили механизм: гейты `check_gate_tests.py` (у гейта есть прогон отвергаемого предмета), `check_generated_sources.py` (у производного файла назван живой исходник) и `check_showcase_links.py` (оригинал не ссылается на свою витрину), плюс хук `PreToolUse` — он отвергает heredoc с экранированием и пуш в чужую ветку, которые CI поймать не может, потому что видит артефакт, а не действие (#1384)
