Упавший джоб матрицы теперь оставляет след: `pytest` пишет junit-отчёт при любом исходе, и он уезжает артефактом с `if: always()`. До этого артефакты выгружались только при успехе — красный `test (windows-latest, 3.12)` на `main` нельзя было даже назвать по имени теста, а логи Actions облачной сессии недоступны (#1375)
