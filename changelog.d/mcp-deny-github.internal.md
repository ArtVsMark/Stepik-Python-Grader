Правило «в GitHub ходим по REST» стало механизмом: `.claude/settings.json` отслеживается git'ом и запрещает окну GitHub-инструменты MCP, которые дублирует `gh_rest.py`, — плюс `ask` на сервер целиком, чтобы инструмент с неучтённым именем не проходил молча. Там же зафиксирован трейлер соавторства, чтобы он ставился харнессом, а не памятью окна (#1286)
