Прогоны CI на `main` перестали отменяться в очереди: на пуш идёт короткий прогон (одна ОС), полная матрица осталась на `pull_request`, а cross-OS покрытие собирается ночным расписанием — прежде шесть мержей подряд давали шесть `cancelled` и ни одного выполненного прогона (#1229)
