Шаг ночного прогона «Задача знает, что породила правило» открывал группировку `{` и не закрывал — bash упал бы на каждом прогоне; заодно `check_workflow_guardrails.py` научился разбирать каждый блок `run:` через `bash -n`, потому что YAML при такой ошибке остаётся валидным (#1384)
