Отвечено на шесть новых правил каталога (177–182), и три из них оказались не декларациями, а проверяемыми утверждениями о нашем дереве: группа отмены в `ci.yml` стояла на `github.ref`, то есть на одном ref для всех коммитов PR — прогон на актуальной голове мог быть вытеснен событием другого коммита, и обязательной проверки на ней не осталось бы вовсе; разборщик скрытых умолчаний брал последнее звено имени, из-за чего псевдоним прятал вызов `subprocess`, а чужой метод `run` ложно краснел; сводка ответа печатала одно число из трёх обязательных. Правило 181 — наше собственное предложение, принятое каталогом, поэтому предложением быть перестало (#1449)
