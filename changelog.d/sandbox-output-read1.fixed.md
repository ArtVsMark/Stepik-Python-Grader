Под `--sandbox` верное решение больше не получает пустой вывод: дренаж потоков в обоих backend'ах песочницы читает `read1` вместо `read`, который отдавал накопленное только по EOF — а решение, оставившее живой дочерний процесс, EOF не даёт. То же исправление, что в `LocalRunner` (#952): туда оно приехало, в `core/sandbox/` — нет (#1247)
