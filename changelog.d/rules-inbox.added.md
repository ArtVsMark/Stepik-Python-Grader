Нерассмотренные правила каталога стали видны в трекере: `scripts/rules_inbox.py` держит один постоянный issue со списком (`unreviewed` плюс те, ответа по которым нет вовсе) и метрикой «сколько дней самому старому» — возраст растёт сам и показывает запущенность, а не объём. Issue находится по скрытому маркеру в теле, поэтому номер нигде не хранится; пишет только по `--apply`, зовёт ночной прогон (#1351)
