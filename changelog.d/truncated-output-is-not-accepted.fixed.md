Обрезка вывода по `max_output_bytes` больше не превращает WA в AC: решение, напечатавшее лишнее сверх потолка, теряло несовпадение вместе с лишним выводом и проходило проверку — теперь у такого прогона вердикт `OLE` (превышен лимит, а не «ответ неверный»), и вывод под потолком по-прежнему не трогается (#1517)
