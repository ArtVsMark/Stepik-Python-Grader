Гейт полноты локалей видит не только веб: `ctx.t(...)` в CLI и `_t(...)` в загрузчике учитываются наравне с `render_message` — 83 из 85 мест вызова были ему невидимы, поэтому переименование ключа оставляло проверку зелёной, а пользователь видел служебный идентификатор вместо текста ошибки (#1142)
