`gh_rest` перестал выдавать отказ по политике за исчерпанную квоту: `403` без заголовков лимита — это другая причина, а не ноль остатка, и она берётся из тела ответа вместо догадки про права токена (#1273)
